apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "jet-app-supervision-gxzh.fullname" . }}
  labels:
    {{- include "jet-app-supervision-gxzh.labels" . | nindent 4 }}
    app: {{ include "jet-app-supervision-gxzh.fullname" . }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "jet-app-supervision-gxzh.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "jet-app-supervision-gxzh.selectorLabels" . | nindent 8 }}
        app: {{ template "jet-app-supervision-gxzh.fullname" . }}
    spec:
      {{- with .Values.image.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          image: {{ include "jet-app-supervision-gxzh.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          envFrom:
            - configMapRef:
                name: {{ include "jet-app-supervision-gxzh.fullname" . }}
            - configMapRef:
                name: {{ include "jet-app-supervision-gxzh.fullname" . }}-action-list
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
