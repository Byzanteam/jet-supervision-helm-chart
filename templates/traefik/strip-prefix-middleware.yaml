apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: {{ include "jet-app-supervision-gxzh.fullname" . }}-strip-prefix
spec:
  stripPrefix:
    prefixes:
      - "/jet-supervision"
